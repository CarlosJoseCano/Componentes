export interface Componente {
  icon: string;
  redirecTo: string;
  name: string;
}

export interface ApiFalseData{
  userId: number;
  id: number;
  title: string;
  body: string;
}

export interface Usuario{
  id: number;
  name: string;
  email: string;
  phone: string;
}
